// blog/static/js/main.js

document.addEventListener('DOMContentLoaded', (event) => {
    // This message confirms the JavaScript file has loaded and executed successfully.
    console.log("Blog main.js script loaded. Time to add interactivity!");

    // Example: Add a class to the header for testing linkage
    document.querySelector('header').classList.add('js-loaded');
})
